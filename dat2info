#!/usr/bin/python

import sys
import struct

def unpack(string, file):
    buffer = file.read(struct.calcsize(string))
    return struct.unpack(string, buffer)

with open(sys.argv[1], "rb") as file:
    print(unpack('iiiii', file))
